import { MainLayout } from '@/template/MainLayout';
import { Calendar } from '@/components/Calendar';

export const Home = () => {
  return (
    <MainLayout>
      <Calendar />
    </MainLayout>
  );
};
